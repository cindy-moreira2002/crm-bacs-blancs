/**
 * Ré-export du prompt système et du schéma JSON des MATHÉMATIQUES — brevet.
 *
 * Voir `src/lib/brevetFrancaisPrompt.ts` : même raison, matière séparée.
 */
export * from '../../supabase/functions/_shared/brevet-maths-prompt';
