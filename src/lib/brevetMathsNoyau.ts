/**
 * Ré-export du noyau de correction des MATHÉMATIQUES — brevet.
 *
 * Voir `src/lib/brevetNoyau.ts` pour la raison de ce ré-export.
 * Ce module ne contient aucune règle de français, et aucune règle du bac.
 */
export * from '../../supabase/functions/_shared/brevet-maths-noyau';
